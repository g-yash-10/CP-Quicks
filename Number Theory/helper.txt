static int factorial(int n){
        int res = 1, i;
        for (i = 2; i <= n; i++)
            res *= i;
        return res;
}

static int gcd(int a, int b){
      if (b == 0)
        return a;
      return gcd(b, a % b);
}
